interface:
  display_name: "Shingan"
  short_description: "Repo-specific patterns and workflows for shingan"
  default_prompt: "Use the shingan repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true