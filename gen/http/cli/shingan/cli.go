// Code generated by goa, DO NOT EDIT.
//
// shingan HTTP client CLI support package
//
// Command:
// $ goa gen github.com/hatyibei/shingan/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	analyzerc "github.com/hatyibei/shingan/gen/http/analyzer/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"analyzer (analyze|health)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "analyzer analyze --body '{\n      \"content\": \"TW9sZXN0aWFlIGF1dCBvbW5pcyBldCBkb2xvcmUgaWxsdW0gaXN0ZS4=\",\n      \"format\": \"json\",\n      \"output\": \"json\"\n   }'" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		analyzerFlags = flag.NewFlagSet("analyzer", flag.ContinueOnError)

		analyzerAnalyzeFlags    = flag.NewFlagSet("analyze", flag.ExitOnError)
		analyzerAnalyzeBodyFlag = analyzerAnalyzeFlags.String("body", "REQUIRED", "")

		analyzerHealthFlags = flag.NewFlagSet("health", flag.ExitOnError)
	)
	analyzerFlags.Usage = analyzerUsage
	analyzerAnalyzeFlags.Usage = analyzerAnalyzeUsage
	analyzerHealthFlags.Usage = analyzerHealthUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "analyzer":
			svcf = analyzerFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "analyzer":
			switch epn {
			case "analyze":
				epf = analyzerAnalyzeFlags

			case "health":
				epf = analyzerHealthFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "analyzer":
			c := analyzerc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "analyze":
				endpoint = c.Analyze()
				data, err = analyzerc.BuildAnalyzePayload(*analyzerAnalyzeBodyFlag)
			case "health":
				endpoint = c.Health()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// analyzerUsage displays the usage of the analyzer command and its subcommands.
func analyzerUsage() {
	fmt.Fprintln(os.Stderr, `Analyze AI agent workflow definitions for structural issues`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] analyzer COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    analyze: Analyze implements analyze.`)
	fmt.Fprintln(os.Stderr, `    health: Health implements health.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s analyzer COMMAND --help\n", os.Args[0])
}
func analyzerAnalyzeUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] analyzer analyze", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Analyze implements analyze.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "analyzer analyze --body '{\n      \"content\": \"TW9sZXN0aWFlIGF1dCBvbW5pcyBldCBkb2xvcmUgaWxsdW0gaXN0ZS4=\",\n      \"format\": \"json\",\n      \"output\": \"json\"\n   }'")
}

func analyzerHealthUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] analyzer health", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Health implements health.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "analyzer health")
}
